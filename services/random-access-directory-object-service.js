(() => {
  'use strict';

  const
    fs = require('fs'),
    path = require('path'),
    exec = require('child_process').exec;

    exports.rados = RandomAccessDirectoryTree;

    function RandomAccessDirectoryTree($done) {
      
    }

})();
